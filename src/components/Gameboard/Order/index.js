import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Order = () => {
    return(
        <View style={{ flex: 1}}>
            <Text>Order</Text>
        </View>
    );
};

export default Order;