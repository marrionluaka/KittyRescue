import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Timer = () => {
    return(
        <View style={{ flex: 1}}>
            <Text>Timer</Text>
        </View>
    );
};

export default Timer;